export const addScore = () => ({
    type: "ADD_SCORE",
  });
  
  export const reset = () => ({
    type: "RESET",
  });
  
  export const changeLevel = (level) => ({
    type: "CHANGE_LEVEL",
    level,
  });